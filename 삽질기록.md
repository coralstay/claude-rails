# 삽질 기록

개인 레포에서 Claude와 함께 작업하다 만난 훅의 구멍, 잘못 짠 안전장치, 헷갈렸던 판단들을
시간순으로 남긴다. 목적은 "잊어버려도 이 파일만 보면 그때 무슨 일이 있었고 왜 이렇게
고쳤는지 복원할 수 있게" 하는 것 — 커밋 메시지나 PR 설명보다 더 서사적으로, 삽질 과정 자체를
남긴다.

---

## 2026-09-10 — `require_active_task.py`가 관계없는 상위 폴더까지 "백로그 프로젝트"로 오판

**증상**: `~/githubs`(git 저장소 아님, 여러 프로젝트를 모아둔 그냥 폴더)에서 아무 파일이나
편집하려 해도 "In Progress 상태인 backlog 태스크가 없습니다"로 막힘. 이 폴더에서는 애초에
backlog.md를 쓴 적도 없는데.

**원인**: `is_backlog_project(cwd)`가 `cwd` 아래 `backlog/config.yml`이 있는지만 봤음.
`~/githubs` 바로 밑에 우연히 `backlog`라는 이름의 서브 레포(backlog.md CLI 연습용 샌드박스)가
있었고, 그 안에 `config.yml`이 있어서 오판했음. `cwd` 자체가 git 레포인지는 아예 확인 안 함.

**고친 방법**: `cwd`에 `.git`이 있는지도 같이 확인하도록 조건 추가.

```python
def is_backlog_project(cwd):
    return (
        os.path.isdir(os.path.join(cwd, ".git"))
        and os.path.isfile(os.path.join(cwd, "backlog", "config.yml"))
    )
```

**교훈**: "특정 이름의 서브폴더가 있다"는 절대 "여기가 그 프로젝트의 루트다"의 증거가 될 수
없다. 항상 "여기 자체가 진짜 그 종류의 루트인가"를 먼저 확인해야 한다.

---

## 2026-09-10 — `pre_git_safety_check.py`가 `gh` 명령에는 아예 안 걸리고 있었음

**증상**: `gh pr merge`를 실행했는데 아무 훅도 안 막음. 원래 이 스크립트 안에
`check_gh_destructive()`라는 함수가 있어서 `gh pr merge`/`gh pr close`/`gh issue close`/
`gh release delete`/`gh repo delete`를 전부 막게 짜여 있는데도.

**원인**: 이 훅의 등록 조건이 `"if": "Bash(git *)"`였음 — 명령어가 `git `로 시작할 때만
스크립트 자체가 실행됨. `gh pr merge`는 `git`이 아니라 `gh`로 시작하니까, 스크립트 안의
로직이 맞고 틀리고를 따질 필요도 없이 **애초에 한 번도 실행되지 않았음**. 코드 리뷰만 하고
실제로 시도해보지 않아서 한동안 "막혀 있다"고 잘못 믿고 있었음(PR 머지를 매번 사람에게
미뤘던 이유).

**어떻게 발견했나**: 실제로 `gh pr merge`를 직접 실행해봤더니 아무 에러 없이 성공함. 그제서야
`if` 조건을 다시 읽고서야 원인을 알았음.

**고친 방법(정책 결정 포함)**: 단순히 `if` 조건만 지우면 `gh pr merge`까지 다시 막혀서, Claude가
PR을 만드는 것까지만 하고 머지는 사람이 하게 되는 원래 의도(이 레포의 "0장" 워크플로)로
되돌아간다는 걸 확인. 논의 끝에 **`gh pr merge`를 포함해 다섯 가지 파괴적 `gh` 명령 전부
차단**하는 쪽으로 확정 — `pr merge`만 예외로 열어두는 중간안도 고려했지만, "운영 레포로의
PR은 사람이 연다"는 이 저장소의 핵심 원칙과 "PR을 사람이 머지한다"는 원칙이 같은 선상에
있다고 판단해서 전부 막는 쪽으로 되돌림. `settings.hooks.json`에서 `pre_git_safety_check.py`
항목의 `if` 조건만 제거 — 스크립트 자체 로직은 이미 맞았으므로 안 건드림.

**교훈**:

1. "코드에 그 로직이 있다"와 "그 로직이 실제로 실행된다"는 다른 문제다 — 트리거 조건(`if`,
   matcher)까지 같이 읽어야 한다.
2. 훅이 막을 거라 믿고 스스로 검증 안 한 채로 "이건 안 된다"고 사용자에게 여러 번 잘못
   안내했음 — 확신이 없으면 실제로 시도해보고 결과로 말해야 한다.
3. 자기 자신의 안전장치를 고치는 작업(`~/.claude/hooks/`, `~/.claude/settings.json`)은
   Claude 스스로 할 수 없게 이중으로 막혀 있음(`config_guard.py` + auto-mode classifier) —
   이건 의도된 설계다. 이런 변경은 항상 사람이 직접 적용해야 한다.

---

<!-- 새 항목은 위에 추가 (최신이 위로 오게) -->
